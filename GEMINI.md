# Picplz 프로젝트 작업 규칙

## 1. 커밋 메시지 규칙

- 모든 커밋 메시지에는 다음 중 하나의 접두사를 사용합니다:
  - `[Feat]`: 새로운 기능 구현
  - `[Design]`: UI/UX 디자인 수정 및 UI 컴포넌트 구현
  - `[Refactor]`: 코드 리팩터링 (기능 변경 없음)
  - `[Fix]`: 버그 수정
  - `[Docs]`: 문서 작성 및 수정
  - `[Chore]`: 빌드 업무, 패키지 매니저 설정 등
  - `[Test]`: 테스트 코드 추가 및 수정
- 커밋 메시지는 한국어로 작성하며, 간결하게 핵심 내용을 전달합니다.
- 예시: `[Feat] 로그인 기능 구현`

## 2. 네비게이션 아키텍처 (TCA)

- **단일 네비게이션 스택 원칙**:
  - 특정 플로우(예: Customer Flow) 내의 모든 뷰는 하나의 루트 `NavigationStack`을 공유해야 합니다.
  - 하위 뷰에서 개별적으로 `StackState`나 `NavigationStack`을 생성하지 않습니다.
- **Delegate 패턴 기반 라우팅**:
  - `path` 상태가 없는 하위 피처는 `delegate` 액션을 통해 부모 피처에게 네비게이션 이벤트를 전달합니다.
  - 부모 피처는 이 `delegate` 액션을 수신하여 자신의 `path`에 새로운 상태를 추가(push)합니다.
- **새로운 루트 정의 시**:
  - 정말로 새로운 네비게이션 루트가 필요한 경우에만 피처에 `path`를 작성하되, 라우팅을 담당하는 뷰와 실제 컨텐츠 뷰를 명확히 분리하여 리팩터링합니다.

## 3. UI 개발 및 디자인 시스템 가이드

### 디자인 시스템 준수 (Figma MCP 연동 시)
- **Raw 데이터 사용 금지**: 컬러 코드(#FFFFFF 등)나 폰트 크기를 코드에 직접 입력하지 않습니다.
- **시스템 활용**: `@Projects/Features/Sources/DesignSystem/**`에 정의된 컬러 시스템과 타이포그래피 시스템을 우선적으로 사용합니다.
- **정의되지 않은 값 처리**: 디자인상 필요하지만 시스템에 정의되지 않은 값이 있다면, 시스템에 추가할지 아니면 해당 뷰에 인라인 멤버로 정의할지 반드시 유저에게 먼저 확인한 후 진행합니다.

### 컴포넌트화 및 디렉토리 구조
- **Massive View 지양**: 하나의 페이지 뷰 파일에 모든 로직을 넣지 말고, 의미 있는 단위로 컴포넌트화하여 조립합니다.
- **계층 구조 및 경로**:
  - **Pages**: 전체 화면 단위의 뷰. 플로우별 루트 디렉토리에 직접 저장합니다. (예: `.../Sources/Customer/SomePage.swift`)
  - **SubViews**: 특정 페이지에서만 쓰이는 중간 규모의 조립 단위. `SubViews/` 디렉토리에 저장합니다.
  - **Components**: 여러 곳에서 재사용 가능한 최소 단위. `Components/` 디렉토리에 저장합니다.
- **플로우 루트 디렉토리**: 기획상의 디자인 플로우에 따라 `Projects/Features/Sources/` 하위에 적절히 서브디렉토링된 패스를 의미합니다. (예: `Customer/`, `Register/`, `Photographer/` 등)
